import { Options } from '../types/options';

export class Client {
  constructor(opt: Options) {
      
  }

  send() {

  }

  catch() {

  }
}

export function setup(options: Options) {
  new Client(options);
}
